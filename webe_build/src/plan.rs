use crate::task::Task;
use crate::planhistory::PlanHistory;

pub struct Plan {
  name: String,
  root_task: Task
}

impl Plan {
  // start executing the plan
  // TODO: needs to return a Future
  fn start() {

  }
}